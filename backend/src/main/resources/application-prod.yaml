spring:
  datasource:
    url: jdbc:mysql://203.57.85.97:3306/RATHNA?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: developer
    password: dev@MSQL25
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: RathnaProductsSecretKeyForJWTTokenGeneration2024MustBe256BitsLong
  expiration: 86400000

server:
  port: 8080
  address: 127.0.0.1
  error:
    include-message: never
    include-binding-errors: never
    include-stacktrace: never

logging:
  level:
    com.rathnaproducts: INFO
    org.springframework.security: WARN
    org.hibernate: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/rathna-products.log