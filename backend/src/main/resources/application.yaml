spring:
  datasource:
    url: jdbc:mysql://203.57.85.97:3306/RATHNA?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: developer
    password: dev@MSQL25
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create | update | validate | none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080